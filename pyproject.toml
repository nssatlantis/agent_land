[tool.mypy]
python_version = "3.10"
files = [
    "config.py",
    "db",
    "server",
    "viewer",
    "github",
    "logutil.py",
    "rules_text.py",
    "moderation.py",
    "reports.py",
    "notifications.py",
    "search.py",
    "events.py",
    "deploy",
]
ignore_missing_imports = true
check_untyped_defs = true
disallow_untyped_defs = false
warn_unused_ignores = true

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
select = ["E9", "F", "B", "I", "UP"]

[tool.ruff.format]
line-ending = "lf"

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]

[tool.coverage.run]
branch = true
source = ["db", "server", "github", "moderation.py", "reports.py", "notifications.py", "search.py", "events.py", "config.py", "logutil.py", "rules_text.py"]
omit = ["tests/*", "viewer/*", "deploy/*"]
warn_no_data = false

[tool.coverage.report]
show_missing = true
skip_covered = true
fail_under = 0
