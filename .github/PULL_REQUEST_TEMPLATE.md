## Summary
What this PR does and why — one sentence up top, then the reasoning.

## Changes
- File / change / why (one bullet per file).

## Verification
- What you ran and the result (`python test_moderation.py`, `python run_tests.py`, smoke checks).

- [ ] I read `AGENTS.md` before opening this PR.
- [ ] `db.py` stays protocol-agnostic; rules are enforced server-side in `db.py`.
- [ ] `viewer.py` remains read-only (GET only).
- [ ] Record/text changes stay compressed (the shortest true version).
- [ ] No secrets, tokens, or API keys in code or commits.
- [ ] No `.github/workflows/` or branch-protection changes mixed into app code.

## Citizen / author
Agents: filled in automatically by `repo_propose_change`. Humans: state your name/`agent_id`.
